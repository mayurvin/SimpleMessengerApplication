# SimpleMessengerApplication
SimpleMessenger app on android enables two android devices to send messages to each other using Java API socket. The size of the message is assumed to be a maximum of 128 bytes. All communication is over TCP.
